# 部署指南

本專案使用雙平台部署架構：
- **Firebase Hosting**（主要部署）：託管實際應用程式
- **Netlify**（代理部署）：提供虛擬網址，代理到 Firebase

---

## 🌐 網址說明

### 真實網址（Firebase Hosting）
- **主要網址**: https://hr-online-training.web.app
- **備用網址**: https://hr-online-training.firebaseapp.com
- **用途**: 直接訪問應用程式

### 虛擬網址（Netlify Proxy）
- **代理網址**: https://miots.netlify.app
- **用途**: 提供易記的網址，內部代理到 Firebase
- **配置檔案**: `netlify.toml` 和 `_proxy/_redirects`

---

## 📦 部署架構

```
┌─────────────────────┐
│  開發者提交程式碼      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   GitHub Repository │
│   (main branch)     │
└──────┬─────────┬────┘
       │         │
       │         └──────────────────┐
       │                            │
       ▼                            ▼
┌──────────────────┐     ┌──────────────────┐
│ GitHub Actions   │     │  Netlify Auto    │
│ (自動部署)        │     │  Deploy (代理)   │
└────────┬─────────┘     └────────┬─────────┘
         │                        │
         ▼                        ▼
┌──────────────────┐     ┌──────────────────┐
│ Firebase Hosting │◄────│ Netlify Proxy    │
│ (真實應用程式)    │     │ (轉發請求)       │
└──────────────────┘     └──────────────────┘
 hr-online-training      miots.netlify.app
 .web.app
```

---

## 🚀 更新流程

### 日常更新（修改程式碼）

1. **本地修改程式碼**
   ```bash
   # 在 VS Code 中編輯檔案
   ```

2. **提交並推送**
   - 在 VS Code 原始檔控制面板
   - 輸入提交訊息
   - 點擊「Commit & Push」

3. **自動部署**
   - GitHub Actions 自動觸發
   - 約 1-2 分鐘後，Firebase Hosting 更新
   - Netlify 自動檢測到變更並重新部署（如果 `netlify.toml` 或 `_proxy/` 有變動）

4. **驗證**
   - 訪問 https://hr-online-training.web.app 查看更新
   - 訪問 https://miots.netlify.app 確認代理正常

---

## 🔧 配置檔案說明

### Firebase 部署配置

#### `.github/workflows/firebase-hosting-merge.yml`
- **用途**: GitHub Actions 自動部署到 Firebase
- **觸發時機**: push 到 main branch
- **部署內容**: 整個專案（除 `.gitignore` 中的檔案）

#### `firebase.json`
```json
{
  "hosting": {
    "public": ".",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"]
  }
}
```

### Netlify 代理配置

#### `netlify.toml`
```toml
[build]
  publish = "_proxy"
  command = "echo 'No build required - this is a proxy configuration'"

[[redirects]]
  from = "/*"
  to = "https://hr-online-training.web.app/:splat"
  status = 200
  force = true
```
- **publish**: 指定發布目錄為 `_proxy`
- **redirects**: 將所有請求（`/*`）代理到 Firebase（`:splat` 保留路徑）
- **force = true**: 強制代理，即使有同名檔案

#### `_proxy/_redirects`（後備配置）
```
/*  https://hr-online-training.web.app/:splat  200!
```
- 作為 `netlify.toml` 的後備
- 格式：`源路徑  目標網址  狀態碼!`
- `!` 表示強制重定向

---

## 🛠 維護指南

### 修改代理目標網址

如果需要將 Netlify 代理指向其他網址：

1. 編輯 `netlify.toml`：
   ```toml
   [[redirects]]
     from = "/*"
     to = "https://新的網址.com/:splat"  # 修改這裡
     status = 200
     force = true
   ```

2. 同時更新 `_proxy/_redirects`：
   ```
   /*  https://新的網址.com/:splat  200!
   ```

3. 提交變更到 GitHub，Netlify 會自動重新部署

### 停用 Netlify 代理

如果不再需要 Netlify 代理：

1. 刪除以下檔案：
   - `netlify.toml`
   - `_proxy/` 目錄

2. 在 Netlify 控制台暫停或刪除部署

### 排查代理問題

1. **檢查 Netlify 部署日誌**
   - 前往 Netlify 控制台
   - 查看最新的部署日誌
   - 確認 `publish` 目錄設定正確

2. **測試代理功能**
   ```bash
   # 比較兩個網址的回應
   curl -I https://miots.netlify.app
   curl -I https://hr-online-training.web.app
   ```

3. **清除快取**
   - 瀏覽器按 `Ctrl + Shift + R` 強制重新整理
   - 或使用無痕模式測試

---

## 📝 注意事項

1. **Netlify 不會自動同步內容**
   - Netlify 只是一個代理層
   - 所有內容實際來自 Firebase Hosting
   - 修改程式碼只需要 push 到 GitHub（Firebase 會自動更新）

2. **`.gitignore` 設定**
   - `firebase-debug.log` 和 `.firebase/` 已被排除
   - `firebase-config.js` 已提交（Firebase Web API Key 可公開）

3. **安全性**
   - Firebase API Key 可安全地暴露在前端（Google 官方說明）
   - 實際的存取控制由 Firebase Security Rules 管理

---

## 🆘 常見問題

**Q: 為什麼需要兩個部署平台？**  
A: Firebase Hosting 是主要平台，託管實際應用。Netlify 提供一個易記的虛擬網址（miots.netlify.app），方便分享和記憶。

**Q: 修改程式碼後，需要等多久才能看到更新？**  
A: 通常 1-2 分鐘。GitHub Actions 完成 Firebase 部署後，內容立即生效。Netlify 代理會即時轉發最新內容。

**Q: 如果 Netlify 掛了會怎樣？**  
A: 用戶仍可以使用真實網址 `https://hr-online-training.web.app` 訪問，不會影響應用程式可用性。

**Q: 可以只使用其中一個平台嗎？**  
A: 可以。如果不需要虛擬網址，可以停用 Netlify。如果只想用 Netlify，可以將整個應用部署到 Netlify（而非代理模式）。

---

## 📞 技術支援

如有部署相關問題，請檢查：
- GitHub Actions 執行紀錄: https://github.com/richchen0228tw/hr-online-training/actions
- Firebase Console: https://console.firebase.google.com/project/hr-online-training
- Netlify Dashboard: 登入 Netlify 查看專案設定
